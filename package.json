{
  "name": "layer0-dynamo-db-example",
  "scripts": {
    "layer0:dev": "layer0 dev",
    "layer0:build": "layer0 build",
    "layer0:deploy": "layer0 deploy",
    "layer0:production": "layer0 run --production",
    "format": "prettier --write '**/*' --ignore-unknown"
  },
  "dependencies": {
    "aws-sdk": "^2.1020.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@layer0/cli": "^4.18.1",
    "@layer0/core": "^4.18.1",
    "@layer0/devtools": "^4.18.5",
    "@layer0/prefetch": "^4.18.1",
    "prettier": "^2.7.1"
  }
}
